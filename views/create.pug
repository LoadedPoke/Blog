doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        title Document
    body
        form(method="POST")
            input(type="text" name="text")
            button(type="submit") Submit